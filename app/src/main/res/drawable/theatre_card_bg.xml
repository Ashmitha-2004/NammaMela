<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <gradient
        android:startColor="@color/primaryLight"
        android:endColor="@color/primaryDark"
        android:angle="270"/>

    <corners android:radius="18dp"/>

    <!-- ✅ Thin border (not cartoonish anymore) -->
    <stroke
        android:width="1dp"
        android:color="@color/goldSoft"/>
</shape>
